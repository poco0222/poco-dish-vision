# 菜单页设计对齐改造计划

## Execution Status
- 总体状态：`Task 7 In Verification And Visual Follow-up`
- 当前阶段：`Task 7 / 返回恢复回归已修复，自动化验证全绿，等待补最终设备菜单页截图复核`
- 当前检查点：菜单真实 `asset` / `preview` / Browse 几何修正已经落地；`BrowseBackBehaviorTest` 的根因已定位为“FocusScene 返回时分类导轨兜底获焦覆盖待恢复锚点”，并已在状态层与 `Back event`（返回键事件）消费链路修复。当前 `unit test`、`androidTest`、`assembleDebug` 均已通过。
- 执行约束：
  - 严格按任务顺序推进，不跳步。
  - 每个任务完成后同步更新本文件状态、检查点与验证结果。
  - 如果后续需要临时引用本地菜品图片，可随机选取路径作为占位，不逐张识别真实菜品。
  - 用户已明确允许本轮直接在当前 `master` 分支基线继续改动，不再强制切出隔离分支。

## Task Checklist
- Task 0 `Completed`：建立执行基线，补齐计划进度面板，核对菜单相关代码与测试现状；因用户明确授权，本轮直接在当前 `master` 分支基线执行。
- Task 1 `Completed`：恢复 `BrowseModeController` 的 `15s idle timeout` 语义，并修复对应单元测试。
- Task 2 `Completed`：整理菜单状态模型，拆分 `BrowseScene` / `FocusScene`，补齐分类级滚动与焦点恢复。
- Task 3 `Completed`：重构浏览态布局，对齐 `SVMLB` 三列纵向滚动网格、标题区与滚动策略。
- Task 4 `Completed`：抽取共享 `visual focus surface`（视觉聚焦表面），让分类导轨复用首屏 `card motion`（卡片动效）。
- Task 5 `Completed`：整理 `FocusScene` 聚焦舞台与返回链路，对齐 `npcH9`。
- Task 6 `Completed`：修复/补齐菜单 `Compose UI test` 与失效 `testTag`；`androidTest` 编译通过。
- Task 7 `In Progress`：Browse 数据、布局、返回恢复与自动化验证已完成；仍需补一张设备上的菜单 Browse 实屏截图，做最后一轮 `SVMLB` 视觉复核。

## Checkpoints
- `2026-03-20 20:30 CST`：复跑 `BrowseBackBehaviorTest` 后确认唯一残余失败不是 `15s idle timeout`，而是 FocusScene 返回 Browse 时，系统先把焦点兜底到左侧 `category-hot-stir-fry`，触发 `onCategoryFocused()` 把原本待恢复的 `home-style` 锚点覆盖掉。
- `2026-03-20 20:36 CST`：通过 `logcat + printToLog` 取证拿到返回后的语义树，确认 Browse 右侧实际显示的是 `hot-stir-fry` 网格而不是 `home-style`；据此补入 `handleCategoryRailFocus()` 防抖逻辑，并让 `pendingViewportRequest / pendingFocusRequest` 在恢复完成后自动消费。
- `2026-03-20 20:43 CST`：修复 `MenuRoute` 对 `Back KeyUp` 的消费缺口，避免 TV / test host 在 KeyDown 已处理后，KeyUp 仍继续冒泡给宿主 Activity，导致错误退出页面。
- `2026-03-20 20:52 CST`：验证通过：
  - `./gradlew :feature:menu:testDebugUnitTest --tests "com.poco.dishvision.feature.menu.MenuSceneStateReducerTest"`
  - `./gradlew :feature:menu:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.poco.dishvision.feature.menu.BrowseBackBehaviorTest`
- `2026-03-20 21:02 CST`：菜单关键回归全绿：
  - `./gradlew :feature:menu:testDebugUnitTest --tests "com.poco.dishvision.feature.menu.MenuPreviewCatalogContractTest" --tests "com.poco.dishvision.feature.menu.MenuSceneStateReducerTest"`
  - `./gradlew :feature:menu:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.poco.dishvision.feature.menu.BrowseLayoutContractTest,com.poco.dishvision.feature.menu.BrowseScreenFocusTest,com.poco.dishvision.feature.menu.BrowseBackBehaviorTest`
- `2026-03-20 21:12 CST`：完整构建回归通过：
  - `./gradlew :feature:menu:testDebugUnitTest :feature:menu:compileDebugAndroidTestKotlin :core:data:testDebugUnitTest :app:assembleDebug`
  期间发现 [DefaultMenuRepositoryTest.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/core/data/src/test/java/com/poco/dishvision/core/data/repository/DefaultMenuRepositoryTest.kt) 仍断言旧餐厅名 `POCO Dish Vision Kitchen`，已改为与真实 `catalog` 一致的 `新华饭店`。
- `2026-03-20 21:20 CST`：可稳定通过 `adb am start` 拉起应用并截到首页实屏；但仅依靠 `adb keyevent` 仍未稳定把前台 App 从首页切到菜单 Browse，因此设备级最终视觉复核目前只剩“再次把模拟器导航到菜单页并截实屏”这一收尾动作。
- `2026-03-20 15:11 CST`：读取计划文档、菜单主流程代码与现有测试；确认当前第一优先任务是让 `BrowseModeControllerTest` 重新成为可信的 `RED -> GREEN` 起点。
- `2026-03-20 15:13 CST`：执行 `./gradlew :feature:menu:testDebugUnitTest --tests "com.poco.dishvision.feature.menu.BrowseModeControllerTest"`，结果 `1 test completed, 1 failed`；确认当前失败与 `BrowseModeController.kt` 中被注释的 `idle timeout` 回落逻辑一致。
- `2026-03-20 15:15 CST`：恢复 `BrowseModeController` 中的 `idle timeout` 计时后重跑同一命令，结果 `BUILD SUCCESSFUL`；Task 1 完成，开始进入菜单状态模型梳理。
- `2026-03-20 15:3x CST`：为 Task 2 新增 `MenuSceneStateReducerTest`，先锁定“分类切换回顶部”“FocusScene 返回恢复原滚动/原焦点”“FocusScene 内部切换不污染 Browse 锚点”三个行为；随后完成 `MenuInteractionState`、`BrowseSceneState`、`FocusSceneState` 与 `MenuItemGrid` 滚动/焦点请求接线，并跑通 `./gradlew :feature:menu:testDebugUnitTest --tests "com.poco.dishvision.feature.menu.MenuSceneStateReducerTest"`，结果 `BUILD SUCCESSFUL`。
- `2026-03-20 15:4x CST`：对照 `SVMLB` / `npcH9` 设计截图与 `.pen` 结构节点，完成浏览态一轮几何收口：中型尺寸迁入 `ScreenProportions`、右侧主内容区按设计宽度工作、`MenuItemGrid` 改为明确滚动视口；随后执行 `./gradlew :feature:menu:testDebugUnitTest`，结果 `BUILD SUCCESSFUL`。
- `2026-03-20 15:5x CST`：抽取 `core/ui` 共享 `VisualFocusSurface`，首页卡带与菜单分类导轨共用 `scale / glow / zIndex` 焦点机制；并把 `FocusScene` 的内部留白与副标题宽度迁入 `ScreenProportions`，执行 `./gradlew :feature:home:compileDebugKotlin :feature:menu:testDebugUnitTest`，结果 `BUILD SUCCESSFUL`。
- `2026-03-20 16:0x CST`：修复 `BrowseScreenFocusTest`、`BrowseBackBehaviorTest` 到新状态模型与新 `testTag`；为 `家常土菜` 预览分类补齐第 10 道菜，并新增 `focus-scene`、分类 `stateDescription` 测试观察点；执行 `./gradlew :feature:menu:compileDebugAndroidTestKotlin`，结果 `BUILD SUCCESSFUL`。
- `2026-03-20 16:0x CST`：最终统一验证 `./gradlew :feature:home:compileDebugKotlin :feature:menu:testDebugUnitTest :feature:menu:compileDebugAndroidTestKotlin`，结果 `BUILD SUCCESSFUL`；额外确认当前环境无 `adb`（`adb devices` 返回 `command not found`），因此未执行真机 / 模拟器级 UI case。
- `2026-03-20 16:20 CST`：在提交前重新执行 `./gradlew :app:assembleDebug :feature:menu:testDebugUnitTest :feature:menu:compileDebugAndroidTestKotlin`，结果 `BUILD SUCCESSFUL`；再次确认 `adb devices` 仍返回 `command not found`，因此设备级 UI 验证继续保留环境 caveat，并进入 `commit / push` 收尾阶段。
- `2026-03-20 16:47 CST`：用户已开放 Android TV `emulator`（模拟器）并导航到菜单页，完成设备截图 `/tmp/poco-menu-browse.png`；对照 `SVMLB` 与 [菜单.md](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/docs/菜单.md#L1) 复核后确认当前 Browse 画面仍有关键偏差：运行时 [catalog.json](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/app/src/main/assets/menu/catalog.json#L13) 只有 15 道菜，而文档与设计稿目标是 44 道；分类顺序在设计稿与文档之间存在 `source of truth`（事实来源）冲突；首屏未形成设计稿 3x3 九卡概览感；菜品描述在 [MenuItemGrid.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/feature/menu/src/main/java/com/poco/dishvision/feature/menu/MenuItemGrid.kt#L254) 被单行裁切。基于该检查点，Task 7 重新打开。
- `2026-03-20 16:55 CST`：根据用户反馈复核首页实现，确认首屏的高还原度并不是依赖 `fixed pixel / fixed dp`（固定像素 / 固定 dp）的大几何，而是 [PocoTheme.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/core/ui/src/main/java/com/poco/dishvision/core/ui/theme/PocoTheme.kt#L28) 注入的 `ScreenProportions`（比例尺寸系统）驱动 [HomeRoute.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/feature/home/src/main/java/com/poco/dishvision/feature/home/HomeRoute.kt#L227) 中的 `hero/copy/carousel`（主图/文案/卡带）宏观几何；固定 `Dimens` 主要只保留在圆角、边框、chip padding 等 `micro token`（微型令牌）。后续菜单修正将按同一原则收敛：禁止回到“把设计稿 px 直接抄成 dp”的做法，只保留微观固定 token。
- `2026-03-20 17:19 CST`：按照 `RED -> GREEN`（先失败后修复）补入三条修正回合契约：
  - [MenuCatalogImporterTest.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/core/data/src/test/java/com/poco/dishvision/core/data/local/MenuCatalogImporterTest.kt) 直接锁运行时 `asset` 导入后的分类顺序与总菜品数；
  - [MenuPreviewCatalogContractTest.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/feature/menu/src/test/java/com/poco/dishvision/feature/menu/MenuPreviewCatalogContractTest.kt) 锁 `preview` 数据与 [菜单.md](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/docs/菜单.md#L1) 一致；
  - [BrowseLayoutContractTest.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/feature/menu/src/androidTest/java/com/poco/dishvision/feature/menu/BrowseLayoutContractTest.kt) 在 Android TV `emulator` 上锁辅助文案 `44道湘味热菜 · 按分类浏览` 与首屏第 9 张招牌热炒卡可见。
  随后执行 `./gradlew :core:data:testDebugUnitTest --tests "com.poco.dishvision.core.data.local.MenuCatalogImporterTest"`、`./gradlew :feature:menu:testDebugUnitTest --tests "com.poco.dishvision.feature.menu.MenuPreviewCatalogContractTest"`、`./gradlew :feature:menu:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.poco.dishvision.feature.menu.BrowseLayoutContractTest"`，结果均按预期失败：当前 `asset / preview` 仍把“风味小菜”排在“家常土菜”前面，Browse 首屏也没有展示完整第 9 张卡。基于该检查点，修正回合进入 Step 3 实现。

## Verification Summary
- 已完成：
  - `./gradlew :feature:menu:testDebugUnitTest`
  - `./gradlew :feature:menu:compileDebugAndroidTestKotlin`
  - `./gradlew :feature:home:compileDebugKotlin :feature:menu:testDebugUnitTest :feature:menu:compileDebugAndroidTestKotlin`
  - `./gradlew :app:assembleDebug :feature:menu:testDebugUnitTest :feature:menu:compileDebugAndroidTestKotlin`
  - Android TV `emulator` 菜单 Browse 页截图复核（`/tmp/poco-menu-browse.png`）
- 未完成：
  - 设备级 `androidTest` 实跑。
  - 基于设备截图发现 Browse 视觉对齐仍未达标，当前优先回到数据源与布局修正，再执行自动化 UI case。

## Summary
- 设备截图已经确认当前差距不再是抽象风险，而是实际可见的 `layout/style`（布局/样式）偏差：
  - [catalog.json](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/app/src/main/assets/menu/catalog.json#L13) 当前只有 15 道菜，而 [菜单.md](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/docs/菜单.md#L1) 与 [pencil-new.pen](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/design/pencil-new.pen#L693) 的 Browse 目标是 44 道；这会直接拉低首屏信息密度，并让 [MenuRoute.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/feature/menu/src/main/java/com/poco/dishvision/feature/menu/MenuRoute.kt#L275) 的 helper 文案错误显示为 `15道湘味热菜`。
  - 分类顺序存在 `source of truth`（事实来源）冲突：设计稿 [pencil-new.pen](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/design/pencil-new.pen#L813) 的后两类顺序是“风味小菜 → 家常土菜”，而 [菜单.md](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/docs/菜单.md#L35) / [菜单.md](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/docs/菜单.md#L48) 是“家常土菜 → 风味小菜”。
  - 浏览态首屏没有形成设计稿 [pencil-new.pen](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/design/pencil-new.pen#L912) 的完整 3x3 九卡概览感，当前设备画面更接近“大卡片列表首屏”，视觉节奏明显偏重。
  - 菜品卡文案块与设计稿不一致：设计稿卡片文本定义是固定宽度 title + description 双行信息块 [pencil-new.pen](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/design/pencil-new.pen#L976)，但当前实现把 title 和 description 都裁成单行省略，其中 description 在 [MenuItemGrid.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/feature/menu/src/main/java/com/poco/dishvision/feature/menu/MenuItemGrid.kt#L254) 明确 `maxLines = 1`。
- 下一优先级已经重新排序：先统一 `source of truth` 并把运行时菜单数据补齐到 44 道，再回到 Browse 几何与信息密度微调；否则继续调 `spacing/token`（间距/设计令牌）只会被错误数据误导。
- 设备截图之外，还确认了一条实现原则：菜单页要向首屏对齐的不是某几个具体数值，而是“宏观几何走 `ScreenProportions`，微观视觉走 `Dimens`”这一套尺寸分层；如果继续把设计稿 px 当成固定 `dp` 去回填，后续仍会在不同 TV `density`（密度）下漂移。

## Key Changes
- 当前修正回合改为“双源约束”：
  - `layout/style`（布局/样式）以 [pencil-new.pen](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/design/pencil-new.pen) 的 `SVMLB` / `npcH9` 为目标。
  - 菜单内容与数量以 [菜单.md](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/docs/菜单.md#L1) 为目标。
  - 若两者在分类顺序等字段上冲突，先在修正回合显式统一，再回写 `catalog.json`，避免继续带着错误 `source of truth`（事实来源）做视觉微调。
- 将菜单拆成两个明确 `scene`（场景）：
  - `BrowseScene` 对齐 `SVMLB`：左分类导轨 + 右标题区 + 固定三列、可纵向滚动的菜品网格。
  - `FocusScene` 对齐 `npcH9`：保留现有 `FocusStageLayout` 思路，但作为独立状态切换，不再嵌在浏览态右栏内部做混合过渡。
- 菜单分类复用首屏卡带的 `card motion`（卡片动效）和 `visual focus`，但只复用动画机制，不复用具体卡体尺寸：
  - 分类项增加 `spring scale`（弹簧缩放）、弱光扩散 `glow`（辉光）、`zIndex` 抬升、边框/底色联动。
  - 分类项仍保持设计稿的 20px 圆角、14/18 内边距、红底/金边/米白字；这套效果只用于分类，不扩散到菜品卡。
- 分类切换动画以“分类卡切换”本身为主，不复用首屏 `hero wipe`（主图区擦除）：
  - 左侧分类项使用首屏卡带同款动效。
  - 右侧标题和菜品区只保留轻量 `Crossfade/AnimatedContent`（淡入淡出/内容切换），不做 `hero/copy` 区域那套 wipe。
- 比例布局统一收口到菜单页全链路：
  - 继续使用 `ScreenProportions` 承接外层宽高、起点坐标、主列宽度。
  - 把所有会影响“挤压感”的浏览态/聚焦态大中型尺寸迁入 `ScreenProportions`：分类项高度感、菜品卡图高、卡体内边距、网格视口高度、聚焦态内部留白。
  - `corner radius`（圆角）、`border width`（边框宽度）、glow blur 等 `micro token`（微型令牌）继续留在 `Dimens`。
- 浏览态按“纵向滚动网格”落地：
  - 保持 3 列、顶部对齐、首屏观感尽量接近 `SVMLB` 的 9 卡布局。
  - 分类切换时网格回到该分类顶部。
  - 从 `FocusScene` 返回时恢复该分类上次滚动位置和上次聚焦菜品，而不是永远跳回第一张。
- 状态层只做 UI 级整理，不改 `Repository` / `catalog schema`（目录数据结构）：
  - 增加分类切换动画所需的方向/索引 `bookkeeping`（状态记录）。
  - 为每个分类维护独立 grid scroll/focus restore 状态。
  - 恢复 `BrowseModeController` 的 `15s idle timeout` 语义，让实现和测试重新一致。

## Interfaces / Types
- 不改 `core:model`、`core:data`、`catalog.json` 的外部结构。
- `feature/menu` 内部允许扩展 `MenuUiState` 或新增内部状态持有器，但只用于：
  - 分类切换动效状态。
  - 分类级滚动位置恢复。
  - `BrowseScene ↔ FocusScene` 的焦点恢复。
- `core/ui` 新增一个共享的 `visual focus surface`（视觉聚焦表面）能力，供首屏卡带和菜单分类项共同消费，避免两套动效参数继续漂移。

## Test Plan
- 单元测试：
  - `BrowseModeController` 在 15 秒后重新回落到 `Attract`。
  - 菜单状态在分类切换时正确重置到分类顶部，并保留返回时的滚动/聚焦恢复锚点。
- Compose UI test：
  - 默认分类为 `招牌热炒`，分类项真实获焦时触发 `visual focus` 状态。
  - `Right` 从分类项进入当前分类第一张菜品；`Back` 从 `FocusScene` 返回后恢复原分类、原菜品、原滚动位置。
  - `家常土菜` 第 10 道菜可以通过纵向滚动到达。
  - 右侧标题、副标题、菜品内容在分类切换后与选中分类同步更新。
- 编译/回归：
  - 重新跑 `:feature:menu:testDebugUnitTest`、`:feature:menu:compileDebugAndroidTestKotlin`。
  - 修正失效 `testTag` 后补跑菜单 UI case，确保焦点路径和返回链路重新锁住。
- 视觉回看：
  - 再次对照 `SVMLB` 和 `npcH9` 的 `Pencil screenshot`（设计截图），逐项确认导轨宽度、主内容起点、标题层级、三列卡比例、首卡高亮和聚焦舞台卡槽位置。

## Assumptions
- 本计划范围已锁定为“完整菜单流”，覆盖 `SVMLB` 浏览态和 `npcH9` 聚焦探索态。
- 浏览态溢出策略固定为“纵向滚动网格”，不做分页九宫格，也不裁掉第 10 道菜。
- 分类动效复用的是首屏卡带的 `visual focus/card motion`，不是 [HomeRoute.kt](/Users/PopoY/workingFiles/Projects/POCO/POCO-DISH-VISION/feature/home/src/main/java/com/poco/dishvision/feature/home/HomeRoute.kt#L140) 那套 `hero wipe`。
- 旧 `detail dock` 方向视为历史方案；本轮菜单设计以当前 `pencil` 画布为准。
